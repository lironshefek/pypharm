
 1. כלי ה-AI וגרסתו
* **כלי וסביבה:** Gemini 



2. הפרומפטים ששימשו ליצירת הנתונים
 א. פרומפט ליצירת קטלוג הפריטים והמיקומים:
> "צור 15–20 רשומות סינתטיות בפורמט JSON Lines עבור מערכת פארם וקוסמטיקה (PyPharm).  
> הנתונים צריכים לכלול מוצרי מדף (מוצרים הדורשים קירור או תוקף, ומוצרים רגילים כגון בשמים ואיפור עם משקל ב-ק"ג), מיקומי סניפים ומחסנים, ועובדים בעלי תפקידים שונים (מנהל לוגיסטיקה, עובד מחסן, רוקח, קופאי). ששמות העובדים יהיו- נועה, לירון, רות ועידן."


 ב. פרומפט ליצירת בקשות הניוד (TransferOrder):
> "צור 15 רשומות סינתטיות של בקשות ניוד בפורמט JSON Lines.  
> כל הזמנה תכלול:
> - order_id ייחודי
> - from_location_id ו-to_location_id (חובה שיהיו שונים זה מזה)
> - priority כמספר שלם בין 1 ל-3 (כאשר 1 מציין עדיפות עליונה)
> - status מתוך הערכים: PENDING, IN_TRANSIT, DELIVERED, CANCELLED
> - handled_by_employee_id (מזהה עובד או null)
> - רשימת items עם מק"טים (sku) וכמויות חיוביות.  
> ודא שכל אובייקט מופיע בשורה נפרדת לחלוטין ללא פסיקים בסוף השורה."



3. בעיות שהתגלו בתוצרי ה-AI
א. **ערבוב ישויות בקובץ יחיד:** בפלט הראשוני התקבל קובץ מעורב שהכיל מוצרים, עובדים ובקשות ניוד יחד. כתוצאה מכך, פונקציית הטעינה של ההזמנות ב-`repository.py` נכשלה עם `KeyError` כיוון שלשורות המוצרים לא היו שדות כמו `from_location_id`. הבעיה נפתרה על ידי הפרדת הנתונים לקבצים ייעודיים.
ב. **חריגה מטווח הוולידציה:** באחת ההזמנות נוצר ערך עדיפות שחרג מסדר העדיפויות הרצוי (`priority: 4`), דבר שהיה משבש את תור העדיפויות (`heapq`). ערך זה תוקן ידנית לעדיפות חוקית.
ג. **מיקומי מקור ויעד זהים:** באחת הרשומות הוגדרו אותו סניף כמקור וכמטרה (`from_location_id == to_location_id`), דבר שגרם לזריקת `ValueError` על ידי הבנאי של `TransferOrder`. השורה עודכנה לסניף יעד שונה.



4. אימות התוצאות והבטחת איכות


א. **התאמה לשדות המחלקות ולמבנה הנתונים:**
   * עברנו ידנית על שורות ה-JSONL ווידאנו שכל שמות המפתחות תואמים במדויק לשמות שהמחלקות מצפות לקבל (כגון `order_id`, `from_location_id`, `to_location_id`, `items`).
   * בדקנו בעין שערכי השדות תקינים מבחינת טיפוסים (מחרוזות שאינן ריקות, כמויות חיוביות, ערכי בוליאן תקניים ומחירים הגיוניים).

ב. **שלמות קשרים ומזהים :**
   * וידאנו ידנית שכל מק"ט (`sku`) שמופיע בפריטי ההזמנות תואם לפריטים הקיימים בקטלוג המוצרים.
   * בדקנו שמזהי העובדים המטפלים תואמים למזהי העובדים שהוגדרו במערכת.

ג. **חוקיות עסקית:**
   * נבדק שבכל הזמנה סניף המקור שונה מסניף היעד, כדי למנוע סתירה עם כללי העסק.
   * אומת שערכי העדיפות (`priority`) נעים בטווח המבוקש (1 עד 3).

ד. **אבטחת מידע ופרטיות:**
   * נבדק שכל המזהים והמיקומים הם פיקטיביים לחלוטין ושלא נעשה שימוש במידע אישי אמיתי.